{% macro path(route, translate, parameters = {}) %}
    {% for key, word in translate %}
        {% set parameters = parameters | merge({(word): (word | trans({}, 'routes'))}) %}
    {% endfor %}

    {{ path(route, parameters)}}
{% endmacro %}
