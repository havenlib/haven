{%  extends "::base.html.twig" %}
{% block inside_content %}
<h2>Afficher la page</h2>

<table class="record_properties">
    <tbody>
        <tr>
            <th>Id</th>
            <td>{{ entity.getId() }}</td>
        </tr>
        <tr>
            <th>Name</th>
            <td>{{ entity.getName() }}</td>
        </tr>
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('evocatio_cms_page_list', {'list': ('list' | trans({}, 'routes'))}) }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('evocatio_cms_page_edit', { 'id': entity.id, 'edit': ('edit' | trans({}, 'routes'))}) }}">
            Edit
        </a>
    </li>
    <li>
        {#<form action="{{ path('EvocatioPersonaBundle_PersonDelete', { 'id': entity.id }) }}" method="post">
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>#}
    </li>
</ul>
{% endblock inside_content %}