{%  extends "::base.html.twig" %}
{% block theatre %}
    <dl>
        {% for entity in entities %}
            <dt><div>{{ entity.status?'Published':'Inactive' }}</div> <strong>{{ entity.username }}</strong> <a href="{{ path('EvocatioSecurityBundle_UserEdit', { 'id': entity.id }) }}">edit</a> <a href="{{ path('EvocatioSecurityBundle_UserShow', { 'id': entity.id }) }}">show</a> </dt>
            <dd>{{ entity.email }}</dd>
        {{ entity.locked }}
        {% else %}
            aucune entré
        {% endfor %}
    </dl>
<ul>
    <li>
        <a href="{{ path('EvocatioSecurityBundle_UserNew') }}">
            Create a new entry
        </a>
    </li>
</ul>
{% endblock theatre %}
