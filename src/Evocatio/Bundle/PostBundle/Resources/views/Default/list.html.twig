{%  extends "::base.html.twig" %}
{% block theatre %}
    <dl>
        {% for entity in entities %}
            <dt><div>{{ entity.status?'Published':'Inactive' }}</div> <strong>{{ entity.question }}</strong> <a href="{{ path('EvocatioPostBundle_edit', { 'id': entity.id }) }}">edit</a> <a href="{{ path('EvocatioPostBundle_show', { 'id': entity.id }) }}">show</a> </dt>
            <dd>{{ entity.reponse }}</dd>
        {{ entity.name }}
        {% else %}
            aucune entré
        {% endfor %}
    </dl>
<ul>
    <li>
        <a href="{{ path('EvocatioPostBundle_new') }}">
            Create a new entry
        </a>
    </li>
</ul>
{% endblock theatre %}
