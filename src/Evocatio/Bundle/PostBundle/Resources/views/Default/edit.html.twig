{%  extends "::base.html.twig" %}
{% block left %}
<form action="{{ path('EvocatioPostBundle_update', { 'id': edit_form.vars.value.id }) }}" method="post" {{ form_enctype(edit_form) }}>
    {{ form_widget(edit_form) }}
<input type="submit" value='go go go' >
</form>
<ul>
    <li>
        <a href="{{ path('EvocatioPostBundle_index') }}">
            Back to the list
        </a>
    </li>
    <li>
        <form action="{{ path('EvocatioPostBundle_delete', { 'id': post.id }) }}" method="post">
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>
{% endblock left %}
{% block right %}
    {% render "EvocatioCoreBundle:Default:i18nSwitcher" %}
{% endblock right %}
