    <dl>
        {% if entity is not empty %}
            {% for line in entity.getPurchaseProducts %}
            <div>{{ line.product.name }}</div> 
            <div>{{ line.price }}</div> 
            {% endfor %}
        {% else %}
            aucune entré
        {% endif %}
            
    </dl>
<ul>
    <li>
        <a href="{{ path('EvocatioPosBundle_BasketEdit') }}">
            edit basket
        </a>
    </li>
</ul>