{%  extends "::base.html.twig" %}
{% block theatre %}
    <dl>
        {% for entity in entities %}
            <dt><div>{{ entity.address }}</div> <a href="{{ path('EvocatioPersonaBundle_PostalEdit', { 'id': entity.id }) }}">edit</a> <a href="{{ path('EvocatioPersonaBundle_PostalShow', { 'id': entity.id }) }}">show</a> </dt>
            <dd></dd>
        {% else %}
            aucune entré
        {% endfor %}
    </dl>
<ul>
    <li>
        <a href="{{ path('EvocatioPersonaBundle_PostalNew') }}">
            Create a new entry
        </a>
    </li>
</ul>
{% endblock theatre %}
