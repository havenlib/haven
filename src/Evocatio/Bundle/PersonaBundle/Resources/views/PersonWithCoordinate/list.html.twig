{%  extends "::base.html.twig" %}
{% block theatre %}
    <dl>
        {% for entity in entities %}
            <div><div>{{ entity.firstname }}</div> <strong>{{ entity.lastname }}</strong> <a href="{{ path('EvocatioPersonaBundle_PersonWithCoorEdit', { 'id': entity.id }) }}">edit</a> <a href="{{ path('EvocatioPersonaBundle_PersonWithCoorShow', { 'id': entity.id }) }}">show</a> </div>
            <div>sex: {{ entity.sex }}</div>
            {% for coor in entity.getCoordinate() %}
            <div>{{ coor.getAddress() }}</div>
            {% endfor %}
        {% else %}
            aucune entré
        {% endfor %}
    </dl>
<ul>
    <li>
        <a href="{{ path('EvocatioPersonaBundle_PersonWithCoorNew') }}">
            Create a new entry
        </a>
    </li>
</ul>
{% endblock theatre %}
