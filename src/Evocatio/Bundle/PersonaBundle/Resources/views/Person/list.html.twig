{%  extends "::base.html.twig" %}
{% block theatre %}
    <dl>
        {% for entity in entities %}
            <div><div>{{ entity.firstname }}</div> <strong>{{ entity.lastname }}</strong> <a href="{{ path('EvocatioPersonaBundle_PersonEdit', { 'id': entity.id }) }}">edit</a> <a href="{{ path('EvocatioPersonaBundle_PersonShow', { 'id': entity.id }) }}">show</a> </div>
            <div>sex: {{ entity.sex }}</div>
            {% for coor in entity.getPostal() %}
            <div>{{ coor.getAddress() }}</div>
            {% endfor %}
            {% for coor in entity.getMap() %}
            <div>{{ coor.getLng() }}</div>
            {% endfor %}
        {% else %}
            aucune entré
        {% endfor %}
    </dl>
<ul>
    <li>
        <a href="{{ path('EvocatioPersonaBundle_PersonNew') }}">
            Create a new entry
        </a>
    </li>
</ul>
{% endblock theatre %}
