{%  extends "::base.html.twig" %}
{% import "::macro/route/suffix.html.twig" as route_suffix %}
{% block inside_content %}
    <dl>
        {% for entity in entities %}
            <div>
                <div> {{ entity.firstname }}</div> 
                <strong>{{ entity.lastname }}</strong> 
                <a href="{{ route_suffix.edit('evocatio_persona_person_edit', 'person', entity.id) }}">edit</a> 
                <a href="{{ route_suffix.show('evocatio_persona_person_show', 'person', entity.id) }}">show</a> 
            </div>
            <div>sex: {{ entity.sex }}</div>
            {% for coor in entity.getPostal() %}
            <div>{{ coor.getAddress() }}</div>
            {% endfor %}
            {% for coor in entity.getMap() %}
            <div>{{ coor.getLng() }}</div>
            {% endfor %}
        {% else %}
            aucune entré
        {% endfor %}
    </dl>
<ul>
    <li>
        <a href="{{ route_suffix.create('evocatio_persona_person_create', 'person') }}">
            Create a new entry
        </a>
    </li>
</ul>
{% endblock inside_content %}
