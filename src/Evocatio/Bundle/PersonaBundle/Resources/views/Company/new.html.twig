{%  extends "EvocatioCoreBundle:Template:add_template.html.twig" %}
{% block formhead %}
    {{ path('evocatio_persona_company_create', {"persona": ("company" | trans)}) }}
{% endblock formhead %}

{% block form %}
<fieldset>
    <ul>
        <li>
            {{ form_label(edit_form.name)}}
            {{ form_errors(edit_form.name)}}
            <span>{{ form_widget(edit_form.name)}}</span>
        </li>
        {{ form_rest(edit_form) }}
        </ul>
{% endblock form %}

{% block back %}
                <a href="{{ path('evocatio_persona_company_list', {"persona": ("company" | trans)}) }}">
                    Back to the list
                </a>
{% endblock back %}
