{% extends "OwnerSiteBundle::base_admin.html.twig" %}
{% block inside_content %}
<h2>Liste des pages</h2>
    <dl>
        {% for entity in entities %}
            <div>
                <div> {{ entity.id }}</div> 
                <a href="{{ _route_crud.path('owner_site_pagecontent_edit', 'edit',entity.id) }}">edit</a> 
                <a href="{{ _route_crud.path('owner_site_pagecontent_show', 'show',entity.id) }}">show</a> 
            </div>
        {% else %}
            aucune entré
        {% endfor %}
    </dl>
<ul>
    <li>
        <a href="{{ _route_crud.path('owner_site_pagecontent_create', 'create') }}">
            Create a new entry
        </a>
    </li>
</ul>
{% endblock inside_content %}
