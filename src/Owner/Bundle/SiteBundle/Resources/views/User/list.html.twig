{% extends "OwnerSiteBundle::base_admin.html.twig" %}
{% block inside_theater %}
    <dl>
        {% for entity in entities %}
            <dt><div>{{ entity.status?'Published':'Inactive' }}</div> <strong>{{ entity.username }}</strong> 
            <a href="{{ _route_suffix.path('owner_site_user_edit', 'edit','user', entity.id) }}">edit</a> 
            <a href="{{ _route_suffix.path('owner_site_user_show', 'show','user', entity.id) }}">show</a> </dt>
            <dd>{{ entity.email }}</dd>
        {{ entity.locked }}
        {% else %}
            aucune entré
        {% endfor %}
    </dl>
<ul>
    <li>
        <a href="{{ _route_suffix.path('owner_site_user_create', 'create','user') }}">
            Create a new entry
        </a>
    </li>
</ul>
{% endblock inside_theater %}
