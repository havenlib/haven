{%  extends "OwnerSiteBundle::base_admin.html.twig" %}
{% block inside_theater %}
<h1>Faq</h1>

<table class="record_properties">
    <tbody>
        <tr>
            <th>Id</th>
            <td>{{ entity.id }}</td>
        </tr>
        <tr>
            <th>Actif</th>
            <td>{{ entity.status }}</td>
        </tr>
        <tr>
            <th>Question</th>
            <td>{{ entity.question }}</td>
        </tr>
        <tr>
            <th>Reponse</th>
            <td>{{ entity.reponse | raw}}</td>
        </tr>
    </tbody>
</table>

        <a href="{{ _route_crud.path('owner_site_faq_edit', 'edit', entity.id) }}">
            Edit
        </a>
   {# 
<ul class="record_actions">
<li>
        <a href="{{ _route_crud.path('owner_site_faq_list', 'list') }}">
            Back to the list
        </a>
    </li>
    <li>
    </li>
   {# <li>
        <form action="{{ path('EvocatioFaqBundle_FaqDelete', { 'id': entity.id }) }}" method="post">
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li> #}
   {# 
</ul>
#}
{% endblock inside_theater %}
{% block aside %}
    {#% render "EvocatioCoreBundle:Core:i18nSwitcher" with {"_locale" : app.getRequest().get("_locale")} %#}
{% endblock aside %}