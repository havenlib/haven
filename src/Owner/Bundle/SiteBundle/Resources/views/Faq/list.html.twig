{%  extends "OwnerSiteBundle::base_admin.html.twig" %}
{% import "::macro/route/i18n.html.twig" as route_i18n %}
{% block inside_content %}
<h2>{{"faq.list" | trans}}</h2>
<table>
    <thead>
        <tr>
            <th>Status</th>
            <th>Title</th>
            <th>Rank</th>
            <th></th>
        </tr>
    </thead>
    <tbody class="sortable">
        {% for entity in entities %}
            <tr>
                <td>{{ entity.status }}</td>
                <td>{{ entity.question }}</td>
                <td>
                    <a class="btn up pull-left" onclick="ranking(this, 'tr');"><img src="/images/arrow_up.png" /></a>
                    <a class="btn down pull-left" onclick="ranking(this, 'tr');"><img src="/images/arrow_down.png" /></a>
                    <form class="form no-margin" action="{{ route_i18n.path('owner_site_faq_rank', {'id': entity.id}, ['rank']) }}" method="POST">
                        <input class="hidden" id="ranktype_rank" type="text" name="rank" value="{{entity.rank}}">
                        <button class="btn hidden pull-left" type="submit" id="ranktype_submit" name="submit">Ok</button>
                    </form>
                </td>
                <td>
                    <a href="{{ path('owner_site_faq_edit', {'edit': ('edit' | trans({}, 'routes')), 'id': entity.id}) }}">edit</a>
                    | <a href="{{ route_i18n.path('owner_site_faq_rank', {'id': entity.id}, ['rank']) }}">ordonner</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td>
                    aucune entré
                </td>
            </tr>
        {% endfor %}
    </tbody>
</table>
    <ul>
        <li>
            <a href="{{ path('owner_site_faq_create', {'create': ('create' | trans({}, 'routes'))}) }}">
                Create a new entry
            </a>
        </li>
    </ul>
{% endblock inside_content %}
