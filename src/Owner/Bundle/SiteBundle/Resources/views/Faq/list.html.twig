{%  extends "OwnerSiteBundle::base_admin.html.twig" %}
{% block inside_theater %}
<dl>
    {% for entity in entities %}
        <dt>
            <div>Status: {{ entity.status?'Published':'Inactive' }}</div> 
            {{ entity.question }}
            <a href="{{ _route_crud.path('owner_site_faq_edit', 'edit', entity.id) }}">edit</a> 
            <a href="{{ _route_crud.path('owner_site_faq_show', 'show',entity.id) }}">show</a> 
        </dt>
        <dd>{{ entity.reponse | raw }}</dd>
    {% else %}
        aucun
    {% endfor %}
</dl>
{#<ul>
    <li>
        <a href="{{ _route_crud.path('owner_site_faq_create', 'create') }}">
            Create a new entry
        </a>
    </li>
</ul>#}
{% endblock inside_theater %}
{% block aside %}
    {#% render "EvocatioCoreBundle:Core:i18nSwitcher" with {"_locale" : app.getRequest().get("_locale")} %#}
{% endblock aside %}


