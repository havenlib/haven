{% extends entity.getTemplate() is empty?"OwnerSiteBundle:Page:create.html.twig":entity.getTemplate().getPath() %}
{% block inside_body %}
{{ parent() }}
{% embed ("OwnerSiteBundle:Page:create.html.twig" ) %}
    {% block formhead %}
        {{ _route_crud.path('owner_site_page_edit', 'edit',entity.id) }}
    {% endblock formhead %}
    {% block form %}<fieldset>
            {{ form_widget(edit_form) }}
    </fieldset>
    {% endblock form %}
{% endembed %}->>
<menu type="toolbar">
    <button type="command" label="Save" onclick="goDaddy()" >go</button>>
</menu>
<<-
<script>
    function goDaddy(){
    var data = CKEDITOR.instances.editable43.getData();
    alert(data);
    }
    // Your code to save "data", usually through Ajax.
</script>
{% endblock inside_body %}


{% block delete %}
    <li>
   {# <form action="{{ path('EvocatioPersonaBundle_PersonDelete', { 'id': entity.id }) }}" method="post">
        {{ form_widget(delete_form) }}
        <button type="submit">Delete</button>
    </form>#}
        </li>
{% endblock delete %}