{%  extends "::base.html.twig" %}
{% block content %}
    <dl>
        {% for entity in entities %}
            <div>{{ entity.name }}</div> <strong>{{ entity.description }}</strong> <a href="{{ path('EvocatioPosBundle_ProductEdit', { 'id': entity.id, 'discriminator' : app.getRequest().get("discriminator") }) }}">edit</a> <a href="{{ path('EvocatioPosBundle_ProductShow', { 'id': entity.id }) }}">show</a>
        {% else %}
            aucune entré
        {% endfor %}
    </dl>
<ul>
    <li>
        <a href="{{ path('EvocatioPosBundle_ProductChooseDiscriminator') }}">
            Create a new entry
        </a>
    </li>
</ul>
{% endblock content %}
