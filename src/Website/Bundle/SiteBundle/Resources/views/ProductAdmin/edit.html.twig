{%  extends "WebsiteSiteBundle:Product:new.html.twig" %}
{% block formhead %}
    {{ path('EvocatioPosBundle_ProductUpdate', { 'id': edit_form.vars.value.id, 'discriminator' : app.getRequest().get("discriminator") }) }}
{% endblock formhead %}
{% block delete %}
<li>
    <form action="{{ path('EvocatioPosBundle_ProductDelete', { 'id': entity.id }) }}" method="post">
        {{ form_widget(delete_form) }}
        <button type="submit">Delete</button>
    </form>
</li>
{% endblock delete %}